import styled from "styled-components";

export const TweetTable = styled.main`
    //border: 1px solid magenta;

    display: flex;
    flex-direction: column;
    justify-content: center;
    text-align: center;
    color: var(--blue);
`;

